---
phase: 03-contact-display-and-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PigeonPlay/Services/ContactsService.swift
  - PigeonPlayTests/ContactsServiceTests.swift
autonomous: true
requirements: [DISPLAY-01, DISPLAY-05, PERM-02, PERM-03]

must_haves:
  truths:
    - "ContactsService.fetchContacts returns .found with CNContact for valid identifiers"
    - "ContactsService.fetchContacts returns .notFound for nonexistent identifiers"
    - "ContactsService.authorizationStatus returns current CNAuthorizationStatus"
    - "ContactsService.requestAccess requests permission when status is .notDetermined"
    - "ContactsService treats .limited same as .authorized for fetch gating"
    - "URL construction strips non-digit characters from phone numbers"
  artifacts:
    - path: "PigeonPlay/Services/ContactsService.swift"
      provides: "All CNContactStore interaction: auth check, auth request, fetch by identifier"
      exports: ["ContactsService", "ContactResult"]
    - path: "PigeonPlayTests/ContactsServiceTests.swift"
      provides: "Unit tests for URL construction, ContactResult handling, auth status branching"
  key_links:
    - from: "PigeonPlay/Services/ContactsService.swift"
      to: "Contacts framework (CNContactStore)"
      via: "import Contacts; CNContactStore.unifiedContact(withIdentifier:keysToFetch:)"
      pattern: "CNContactStore.*unifiedContact"
---

<objective>
Create the ContactsService enum (all CNContactStore interaction) and its test suite. This is the foundation that Plan 02 builds on.

Purpose: Isolate all Contacts framework interaction into a single service following the LineSuggester enum pattern. Tests cover the logic we can unit-test (URL construction, ContactResult handling, auth status branching) since CNContactStore itself requires device contacts for integration testing.

Output: ContactsService.swift in Services/, ContactsServiceTests.swift in PigeonPlayTests/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-contact-display-and-actions/03-RESEARCH.md

@PigeonPlay/Services/LineSuggester.swift
@PigeonPlay/Models/Player.swift
@PigeonPlayTests/ContactPickerRepresentableTests.swift

<interfaces>
<!-- Existing patterns the executor needs -->

From PigeonPlay/Services/LineSuggester.swift:
```swift
// Service pattern: enum with static methods, no @Observable
enum LineSuggester {
    static func suggest(available: [Player], ...) -> LineSuggestion? { ... }
}
```

From PigeonPlay/Models/Player.swift:
```swift
@Model final class Player {
    var contactIdentifiers: [String] = []
    var phoneNumber: String?
}
```

From PigeonPlayTests/ContactPickerRepresentableTests.swift:
```swift
// Test pattern: Swift Testing @Test macro, @MainActor for UIKit-touching code
import Testing
@testable import PigeonPlay

@Test @MainActor func coordinatorInvokesOnSelectWithIdentifier() { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ContactsService and ContactResult with TDD tests</name>
  <files>PigeonPlay/Services/ContactsService.swift, PigeonPlayTests/ContactsServiceTests.swift</files>
  <read_first>
    - PigeonPlay/Services/LineSuggester.swift (enum service pattern to follow)
    - PigeonPlayTests/ContactPickerRepresentableTests.swift (test pattern with @Test macro)
    - .planning/phases/03-contact-display-and-actions/03-RESEARCH.md (Pattern 1: ContactsService, Code Examples section)
    - project.yml (to verify source directories auto-include new files)
  </read_first>
  <behavior>
    - Test: ContactResult.found stores a CNContact; ContactResult.notFound stores a String identifier
    - Test: authorizationStatus returns the current CNContactStore.authorizationStatus(for: .contacts) value
    - Test: canFetch returns true for .authorized and .limited, false for .denied and .restricted and .notDetermined
    - Test: keysToFetch contains exactly CNContactGivenNameKey, CNContactFamilyNameKey, CNContactPhoneNumbersKey, CNContactEmailAddressesKey
    - Test: String.digitsOnly on "(555) 867-5309" returns "5558675309"
    - Test: String.digitsOnly on "+1 555-867-5309" returns "15558675309"
    - Test: String.digitsOnly on "" returns ""
    - Test: tel URL from digits "5558675309" produces "tel://5558675309"
    - Test: sms URL from digits "5558675309" produces "sms://5558675309"
    - Test: mailto URL from "coach@example.com" produces "mailto:coach@example.com"
    - Test: tel URL from empty string returns nil
  </behavior>
  <action>
    RED phase: Create PigeonPlayTests/ContactsServiceTests.swift with failing tests covering all behaviors above. Use Swift Testing @Test macro. Tests for `canFetch` can use a helper that takes a `CNAuthorizationStatus` parameter to avoid needing to mock the store. Tests for URL construction use a static helper function (e.g., `ContactsService.callURL(phone:)`, `ContactsService.smsURL(phone:)`, `ContactsService.emailURL(address:)`) that returns `URL?`. The `digitsOnly` test covers a `String` extension.

    Run tests — they must all FAIL (types do not exist yet).

    GREEN phase: Create PigeonPlay/Services/ContactsService.swift with:

    1. `import Contacts` at top
    2. `enum ContactResult` with cases `.found(CNContact)` and `.notFound(String)`
    3. `enum ContactsService` with:
       - `static let keysToFetch: [CNKeyDescriptor]` containing exactly: `CNContactGivenNameKey as CNKeyDescriptor`, `CNContactFamilyNameKey as CNKeyDescriptor`, `CNContactPhoneNumbersKey as CNKeyDescriptor`, `CNContactEmailAddressesKey as CNKeyDescriptor`
       - `static func authorizationStatus() -> CNAuthorizationStatus` — returns `CNContactStore.authorizationStatus(for: .contacts)`
       - `static func canFetch(status: CNAuthorizationStatus) -> Bool` — returns true for `.authorized` and `.limited`, false otherwise. Include `@unknown default` returning false.
       - `@MainActor static func requestAccess() async -> Bool` — creates `CNContactStore()`, calls `try? await store.requestAccess(for: .contacts)`, returns result `?? false`
       - `@MainActor static func fetchContacts(identifiers: [String]) async -> [ContactResult]` — guard empty returns []. Creates CNContactStore. Maps over identifiers: `try store.unifiedContact(withIdentifier: id, keysToFetch: keysToFetch)` wrapped in do/catch. Catch `CNError` where `.code == .recordDoesNotExist` returns `.notFound(id)`. Generic catch also returns `.notFound(id)`.
       - `static func callURL(phone: String) -> URL?` — `let digits = phone.digitsOnly; guard !digits.isEmpty else { return nil }; return URL(string: "tel://\(digits)")`
       - `static func smsURL(phone: String) -> URL?` — same pattern with "sms://"
       - `static func emailURL(address: String) -> URL?` — `guard !address.isEmpty else { return nil }; return URL(string: "mailto:\(address)")`

    4. Add `String` extension with `var digitsOnly: String { filter(\.isNumber) }` — put this in ContactsService.swift (small, single-use).

    Both methods `requestAccess` and `fetchContacts` are `@MainActor` because CNContact is not Sendable (Swift 6 strict concurrency — see Pitfall 4 in research).

    Run `xcodegen generate` in the project root to pick up the new files, then run tests — all must PASS.

    REFACTOR: Review for any cleanup. Ensure no duplication in URL construction helpers.
  </action>
  <verify>
    <automated>cd /Users/robin/workspace/roster-manager && xcodegen generate && xcodebuild test -project PigeonPlay.xcodeproj -scheme PigeonPlay -destination 'platform=iOS Simulator,name=iPhone 16e' 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "enum ContactsService" PigeonPlay/Services/ContactsService.swift
    - grep -q "enum ContactResult" PigeonPlay/Services/ContactsService.swift
    - grep -q "case found(CNContact)" PigeonPlay/Services/ContactsService.swift
    - grep -q "case notFound(String)" PigeonPlay/Services/ContactsService.swift
    - grep -q "CNContactGivenNameKey" PigeonPlay/Services/ContactsService.swift
    - grep -q "CNContactFamilyNameKey" PigeonPlay/Services/ContactsService.swift
    - grep -q "CNContactPhoneNumbersKey" PigeonPlay/Services/ContactsService.swift
    - grep -q "CNContactEmailAddressesKey" PigeonPlay/Services/ContactsService.swift
    - grep -q "@MainActor" PigeonPlay/Services/ContactsService.swift
    - grep -q "var digitsOnly" PigeonPlay/Services/ContactsService.swift
    - grep -q "func callURL" PigeonPlay/Services/ContactsService.swift
    - grep -q "func smsURL" PigeonPlay/Services/ContactsService.swift
    - grep -q "func emailURL" PigeonPlay/Services/ContactsService.swift
    - grep -q "canFetch" PigeonPlay/Services/ContactsService.swift
    - grep -q ".limited" PigeonPlay/Services/ContactsService.swift
    - grep -c "@Test" PigeonPlayTests/ContactsServiceTests.swift returns 11 or more
    - xcodebuild test succeeds with all tests passing
  </acceptance_criteria>
  <done>ContactsService enum exists with all CNContactStore methods, URL helpers, and digitsOnly extension. ContactsServiceTests covers canFetch branching, URL construction, and digitsOnly normalization. All tests pass under Swift 6 strict concurrency.</done>
</task>

</tasks>

<verification>
- `xcodebuild test -project PigeonPlay.xcodeproj -scheme PigeonPlay -destination 'platform=iOS Simulator,name=iPhone 16e'` passes all tests
- ContactsService.swift contains no direct UI code (no SwiftUI imports)
- ContactsService follows LineSuggester pattern (enum, static methods)
</verification>

<success_criteria>
- ContactsService and ContactResult types exist and compile under Swift 6
- All URL construction tests pass with correct normalization
- canFetch correctly handles all CNAuthorizationStatus cases including .limited
- keysToFetch contains exactly the four required keys
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-contact-display-and-actions/03-01-SUMMARY.md`
</output>
